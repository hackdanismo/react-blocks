# React Blocks

## Setup Vite

```shell
# Setup React using Vite. Follow instructions in the CLI.
$ npm create vite@latest
# Once installed, run the development server and open the app: http://localhost:5173/
$ npm run dev
```